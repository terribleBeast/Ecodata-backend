[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.0",
    "torch",
    "uvicorn>=0.44.0",
]

[tool.uv.sources]
torch = { path = "../../../Downloads/torch-2.0.1+cu117-cp310-cp310-win_amd64.whl" }
